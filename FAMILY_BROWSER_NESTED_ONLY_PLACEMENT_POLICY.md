# KKY Family Browser - 하위 전용 패밀리 단독 모델링 제한 정책

Last updated: 2026-07-15 18:38 KST

## 목적

표준 RVT에서 다른 상위 패밀리의 구성요소로만 사용하는 하위 패밀리를 사용자가 프로젝트에 단독으로 배치하지 못하게 한다. 상위 패밀리를 정상 배치할 때 내부에서 생성되는 하위 패밀리 인스턴스는 허용한다.

## 적용 범위

- `권한 / 차단 > 파일별 권한 적용 설정`에서 RVT 파일별로 켜고 끈다.
- 설정명은 `하위 전용 패밀리 단독 모델링 금지`이다.
- `관리자 ON`에서는 우회되고, `관리자 OFF`에서만 적용된다.
- 이 설정만 켠 경우 패밀리 파일 로드, 패밀리/타입 이름 변경 자체는 막지 않는다. 해당 제한은 기존 `패밀리 로드/편집`, `타입 변경` 설정이 담당한다.
- 이미 프로젝트에 존재하는 인스턴스를 소급 삭제하지 않는다. 새로 추가되는 단독 인스턴스만 커밋 전에 차단한다.

## 하위 전용 판정 기준

현재 표준 관리에서 선택된 공종/표준 RVT의 최신 정밀 스캔을 기준으로 아래 조건을 모두 만족하면 하위 전용 패밀리로 판정한다.

1. 한 개 이상의 상위 패밀리에서 하위 로더블 패밀리로 참조된다.
2. 표준 RVT 프로젝트 레벨에서 상위가 없는 단독 인스턴스가 0개다.
3. 카테고리 ID 또는 카테고리명과 패밀리명이 일치한다.
4. 정밀 스캔의 하위 관계 및 단독 배치 사용량 메타데이터가 완전하다.

기존 스냅샷에는 단독 배치 사용량 정보가 없으므로 오탐 방지를 위해 차단하지 않는다. 기능을 켠 뒤 해당 표준 RVT를 새 버전으로 정밀 스캔해야 한다.

## 허용/차단 규칙

| 작업 | 결과 | 이유 |
|---|---|---|
| 상위 패밀리 A 배치, 내부에서 하위 a/b/c 생성 | 허용 | 생성된 하위 인스턴스에 상위 구성요소가 존재함 |
| 하위 전용 패밀리 c를 프로젝트에 직접 배치 | 차단 | 새 인스턴스의 `SuperComponent`가 없음 |
| 하위 전용 패밀리 c의 단독 인스턴스를 복사/붙여넣기 | 차단 | 프로젝트에 새 단독 인스턴스가 추가됨 |
| 표준 RVT에서 하위로도 쓰이고 단독 인스턴스도 존재하는 패밀리 | 허용 | 하위 전용이 아님 |
| 관리자 ON 상태에서 직접 배치 | 허용 | 관리 작업 우회 |
| 정밀 스캔 정보가 없거나 손상됨 | 허용 + 진단 대상 | 잘못된 차단보다 실패 개방을 우선함 |

## 데이터와 성능

- 정밀 스캔 스냅샷 스키마는 `6`이다.
- 스냅샷 저장 시 `<snapshot>.nested-only-placement-v1.json` 카탈로그를 함께 만든다.
- 카탈로그는 패밀리명, 카테고리, 상위 패밀리명만 담는 경량 파일이다.
- `%LOCALAPPDATA%\KKY\FamilyBrowser\Cache\Guard\NestedOnly`에 검증된 로컬 캐시를 둔다.
- 관리 폴더가 연결된 경우 스냅샷 수정 시각과 크기가 일치하는 카탈로그만 사용한다.
- 관리 폴더가 오프라인이면 마지막 검증 캐시를 읽을 수 있다.
- 전체/선택 정밀 스캔 결과가 연결되면 이전 카탈로그 캐시를 즉시 비워 새 판정이 다음 배치부터 바로 적용된다.

## 차단 기록

차단 시 기존 File Guard 감사 로그에 다음 의미값을 남긴다.

- `Kind`: `NestedOnlyFamilyPlacement`
- `State`: `UnauthorizedNestedOnlyStandalonePlacement`
- `PolicyReason`: `FamilyIsNestedOnlyInSelectedStandard`
- `ParentFamilyNames`: 해당 하위 패밀리를 사용하는 표준 상위 패밀리 목록
- `RequiredAction`: 상위 패밀리를 배치하거나 관리자에게 문의

## Revit 실기 확인

1. 표준 RVT에 상위 A와 하위 a/b/c를 만들고 c의 단독 인스턴스는 두지 않은 채 정밀 스캔한다.
2. 대상 프로젝트를 File Guard에 등록하고 `하위 전용 패밀리 단독 모델링 금지`를 켠다.
3. 관리자 OFF에서 A 배치가 성공하고 내부 a/b/c가 유지되는지 확인한다.
4. c 직접 배치와 단독 c 복사/붙여넣기가 커밋 전에 취소되는지 확인한다.
5. 관리자 ON에서 c 직접 배치가 허용되는지 확인한다.
6. 표준 RVT에 c 단독 인스턴스를 추가해 다시 정밀 스캔한 뒤 관리자 OFF에서도 c 직접 배치가 허용되는지 확인한다.
7. 중앙 파일과 이름이 다른 로컬 파일에서 동일하게 적용되는지 확인한다.

## 자동 검증 결과

- 보고서: `artifacts\family-browser-ui-audit\20260715-nested-only-placement-final-v3\quality-gate-summary.md`
- 정적/계약 검사: 통과
- Revit 2019/2021/2023/2025/2027 Release 빌드 및 Stage 검증: 통과
- 2,000행 목록 성능/캐시 검사: 통과
- IE WebBrowser 클릭·언어·레이아웃 회귀: `136 OK`, 실패 `0`
- Revit 2021 런타임: 이 PC에 설치되지 않아 `1 SKIP`; 빌드와 Stage 검증은 통과
- ProgramData 설치본과 설치파일: 이번 작업에서는 변경하지 않음
- 실제 Revit 트랜잭션에서의 상위 배치 허용/하위 단독 배치 롤백은 위 실기 확인 절차가 남아 있음
